version: "3.9"

services:
  voicegen:
    build: .
    env_file: .env
    volumes:
      - ./temp:/app/temp
    restart: unless-stopped
    networks:
      - genmanager_net

networks:
  genmanager_net:
    external: true
